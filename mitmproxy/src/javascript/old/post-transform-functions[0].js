functions[0] = function () {
    var stateContext = beforeFunctionState("functions[0]", globalState);
    var tapeLength;
    tapeLength = tape.length, globalState[35] = tapeLength;
    afterFunctionState(stateContext, globalState);
}